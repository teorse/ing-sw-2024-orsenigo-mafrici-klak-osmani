package it.polimi.ingsw.Server.Model.Lobby;

/**
 * Enum representing the roles that a user can have in a lobby.
 * ADMIN indicates that the user has administrative privileges in the lobby.
 * GUEST indicates that the user has guest privileges in the lobby.
 */
public enum LobbyRoles {
    ADMIN,
    GUEST
}
