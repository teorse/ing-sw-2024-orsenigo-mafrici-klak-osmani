/**
 * Enum class that contains all allowed values for a corner.<br>
 * corners reference this class when defining how they can be interacted with.
 */

public enum CornerType {EMPTY,NULL,ITEM,RESOURCE}
