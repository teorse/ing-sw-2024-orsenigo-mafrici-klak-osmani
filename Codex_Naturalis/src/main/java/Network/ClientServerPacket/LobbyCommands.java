package Network.ClientServerPacket;

/**
 * The LobbyCommands enum represents the allowed values for commands related to the Lobby layer in ClientServerMessage header.
 * These commands are placeholders for the demo and may not represent actual lobby functionality.
 */
public enum LobbyCommands {
    START_GAME,
    QUIT,
    CHANGE_COLOR,

    //DEMO PURPOSES
    COMMAND1,
    COMMAND2,
    COMMAND3
}
